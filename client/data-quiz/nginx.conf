server {
    listen 80;
    listen [::]:80;
    server_name geekonomy.eu www.geekonomy.eu;
    server_name 51.83.133.252;

    access_log  /var/log/nginx/geekonomy.eu.access.log  main;
    error_log  /var/log/nginx/geekonomy.eu.error.log;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
    
    location / {
        root   /usr/share/nginx/html;
        #index  index.html index.htm;
        try_files $uri $uri/ /index.html =404;
    }

    location /api {
        rewrite /api/(.*) /$1 break;
        proxy_pass http://api;
    }
}

# server {
#     listen [::]:443 ssl ipv6only=on;
#     listen 443 ssl;
#     server_name geekonomy.eu www.geekonomy.eu; 
#     server_name 51.83.133.252;

#     ssl_certificate /etc/letsencrypt/live/geekonomy.eu/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/geekonomy.eu/privkey.pem;

#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

#     access_log  /var/log/nginx/geekonomy.eu.access.log  main;
#     error_log  /var/log/nginx/geekonomy.eu.error.log;

#     location / {
#         root   /usr/share/nginx/html;
#         try_files $uri /index.html =404;
#     }

#     location /api {
#         rewrite /api/(.*) /$1 break;
#         proxy_pass http://api;
#     }
# }